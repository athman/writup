<%= form_for(current_user.relationships.find_by(followed_id: @user.id)), html: { method: "delete" } do |f| %>
  <%= button_tag(type: "submit", class: "btn btn-lg btn-primary btn-danger") do %>
    Unfollow <span class="glyphicon glyphicon-minus"></span>
  <% end %>
<% end %>